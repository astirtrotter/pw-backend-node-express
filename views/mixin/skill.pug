mixin skill(skill)
  +skillTd('', skill.name)
  td
    img.img-fluid.portrait-2(src=`/assets/skills/${skill._id}`, onerror=`this.src='/assets/skills/_default'`)
  +skillTd(skill.type, skill.type)

mixin skillTd(type, text)
  td(class='text-' + (type === 'Database' ? 'danger' : type === 'Language' ? 'primary' : type === 'Library/Framework' ? 'warning' : type === 'Platform' ? 'info' : type === 'Tool' ? 'success' : 'muted'))
    = text