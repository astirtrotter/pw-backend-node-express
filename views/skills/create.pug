extends ../default

block content
  h1 #{title}
  .d-flex.flex-column.mt-5
    form(method='POST', action='/api/skills')
      .hovereffect
        img.border.img-fluid.rounded-circle.portrait-1(onerror=`this.src='/assets/skills/_default.png'`)
        .overlay.portrait-1.rounded-circle
          label.info Change
      input.d-none(type='file', name='image', accept='image/png, image/jpeg')